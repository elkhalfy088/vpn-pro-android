{
  "rules": {
    "servers": {
      ".read": true,
      ".write": true,
      "$serverId": {
        ".validate": "newData.hasChildren(['name','endpoint','serverPublicKey','clientPrivateKey','clientAddress'])"
      }
    }
  }
}
