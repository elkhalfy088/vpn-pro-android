<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Base theme for the app window shell.
        Compose renders all UI, so we just need a bare AppCompat/Material3 no-action-bar window.
        Theme.AppCompat.Light.NoActionBar is provided by the material/appcompat dependency
        and is guaranteed to work with compileSdk 35 + AAPT2.
    -->
    <style name="Theme.VpnPro" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
